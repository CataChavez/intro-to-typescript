(function(){ 
  
  function getAge(){
    return 10+10+13;
  }

  const name = 'Fernando';
  const lastName = 'Herrera';
  const edad = 33;

  const output = `${name} ${lastName} (${ getAge() })`;

  console.log(output)
})();

