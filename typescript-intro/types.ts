(function(){ 
  let message : string = "hola";
  let number : number = 123;
  let boolean : boolean = true;
  let today : Date = new Date();

  let anything;
  anything = message;
  anything = number;
  anything = boolean;
  anything = today;

  let spiderman = {
    name: 'Peter',
    edad: 30
  }

  spiderman = {
    name: 'Juan',
    edad: 30,
  }

})();

